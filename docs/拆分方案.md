# PVE 多用户控制面板 server.js 完整拆分规划
## 一、拆分原则与目标
### 核心原则
1. **职责单一**：每个文件只做一件事
2. **分层清晰**：入口→路由→服务→工具，层级分明
3. **渐进式拆分**：先拆独立模块，再拆核心业务，最后精简入口
4. **零风险**：拆分过程中保持功能完全不变，每一步都可回滚

### 预期目标
- 将 2000+ 行的 `server.js` 拆分为 20+ 个小文件
- 入口文件 `server.js` 精简到 100 行以内
- 所有业务逻辑按模块归类，查找修改效率提升 10 倍
- 保留 100% 原有功能，无任何业务变更

## 二、最终目录结构
```
项目根目录
├── server.js                     # 仅保留服务启动与初始化
├── config/                       # 配置层
│   ├── env.js                    # 环境变量与全局常量
│   └── multer.js                 # 文件上传配置
├── middleware/                   # 中间件层
│   ├── auth.js                   # 认证与权限中间件
│   └── error.js                  # 全局错误处理中间件（新增）
├── utils/                        # 工具函数层
│   ├── debug.js                  # 调试日志
│   ├── token.js                  # Token 生成与验证
│   ├── email.js                  # 邮件发送与模板
│   ├── pve-rate.js               # PVE 流量速率计算
│   ├── site-url.js               # 网站 URL 获取
│   └── cdk-generator.js          # CDK 码生成工具
├── routes/                       # 路由层（按业务模块拆分）
│   ├── auth.js                   # 认证相关路由（登录/登出/刷新）
│   ├── user.js                   # 用户中心路由（个人信息/邮箱/密码）
│   ├── admin-user.js             # 管理员用户管理路由
│   ├── vm.js                     # 虚拟机管理路由
│   ├── lxc.js                    # LXC 容器管理路由
│   ├── snapshot.js               # 快照管理路由（VM+LXC）
│   ├── backup.js                 # 备份与恢复路由（VM+LXC）
│   ├── cdk.js                    # CDK 兑换码路由
│   ├── message.js                # 站内消息路由
│   ├── admin-config.js           # 管理员系统配置路由
│   └── network.js                # 网络配置与端口转发路由
├── services/                     # 服务层（复杂业务逻辑）
│   ├── expiry-check.js           # 到期检查与提醒服务
│   ├── backup-polling.js         # 备份/恢复进度轮询服务
│   ├── ikuai-sync.js             # 爱快数据同步服务
│   └── dhcp-static.js            # DHCP 静态绑定服务
├── websocket/                    # WebSocket 代理层
│   ├── vnc-proxy.js              # VNC 代理
│   └── terminal-proxy.js         # xterm.js 终端代理
├── schedule/                     # 定时任务层
│   └── tasks.js                  # 所有定时任务定义
├── api/                          # 原有 API 层（保持不变）
│   ├── db-sqlite.js
│   ├── pve-api.js
│   ├── ikuai-api.js
│   └── ssh-exec.js
├── public/                       # 前端静态文件（不变）
├── images/                       # 上传文件目录（不变）
├── .env                          # 环境变量（不变）
└── package.json                  # 依赖（不变）
```

## 三、分步拆分实施指南
### 第一步：拆分独立工具函数（零风险，优先做）
**目标**：把所有不依赖 `app` 实例的纯工具函数抽离出去

#### 1. `utils/debug.js`
```javascript
const DEBUG = process.env.DEBUG === 'true';
const dbg = (...args) => {
  if (DEBUG) console.log('[DEBUG]', ...args);
};

module.exports = dbg;
```

#### 2. `utils/pve-rate.js`
```javascript
const pveCounterCache = new Map();

function _applyRate(key, statusData) {
  // 原代码完整复制，一字不改
  if (!statusData || typeof statusData !== 'object') return statusData;
  const fields = ['netin', 'netout', 'diskread', 'diskwrite'];
  const hasCounter = fields.some(f => typeof statusData[f] === 'number');
  if (!hasCounter) return statusData;

  const now = Date.now();
  const prev = pveCounterCache.get(key);
  const result = { ...statusData };

  if (prev && prev._timestamp && (now - prev._timestamp) > 0) {
    const dt = (now - prev._timestamp) / 1000;
    for (const field of fields) {
      if (typeof statusData[field] === 'number' && typeof prev[field] === 'number') {
        const diff = statusData[field] - prev[field];
        result[field] = diff >= 0 ? diff / dt : 0;
      }
    }
  } else {
    for (const field of fields) {
      if (typeof statusData[field] === 'number') {
        result[field] = 0;
      }
    }
  }

  const entry = { _timestamp: now };
  for (const field of fields) {
    if (typeof statusData[field] === 'number') {
      entry[field] = statusData[field];
    }
  }
  pveCounterCache.set(key, entry);
  return result;
}

// 新增：缓存清理函数
function clearExpiredCache() {
  const expire = Date.now() - 2 * 3600 * 1000;
  for (const [key, val] of pveCounterCache) {
    if (val._timestamp < expire) {
      pveCounterCache.delete(key);
    }
  }
}

module.exports = { _applyRate, clearExpiredCache };
```

#### 3. `utils/email.js`
```javascript
const nodemailer = require('nodemailer');
const db = require('../api/db-sqlite');

// 原 createEmailTemplate 函数完整复制
function createEmailTemplate(title, content) {
  return `
    <!DOCTYPE html>
    <html lang="zh-CN">
    <!-- 原 HTML 模板完整复制，一字不改 -->
    </html>
  `;
}

// 原 sendEmail 函数完整复制
async function sendEmail(to, subject, html) {
  const config = db.config.getSmtp();
  
  if (!config.enabled || !config.host || !config.user || !config.password) {
    throw new Error('SMTP 配置不完整或未启用');
  }
  
  try {
    const transporter = nodemailer.createTransport({
      host: config.host,
      port: config.port,
      secure: config.secure,
      auth: {
        user: config.user,
        pass: config.password
      },
      tls: {
        rejectUnauthorized: false
      }
    });
    
    await transporter.verify();
    
    const mailOptions = {
      from: config.from || config.user,
      to: to,
      subject: subject,
      html: html
    };
    
    return await transporter.sendMail(mailOptions);
  } catch (error) {
    console.error('发送邮件失败:', error);
    throw new Error('邮件发送失败: ' + error.message);
  }
}

module.exports = { createEmailTemplate, sendEmail };
```

#### 4. `utils/token.js`
```javascript
const jwt = require('jsonwebtoken');
const CryptoJS = require('crypto-js');
const db = require('../api/db-sqlite');

const JWT_SECRET = process.env.JWT_SECRET || CryptoJS.lib.WordArray.random(32).toString();
const JWT_EXPIRES_IN = '15m';
const REFRESH_TOKEN_DAYS = 7;

function generateToken() {
  return CryptoJS.lib.WordArray.random(32).toString();
}

function generateAccessToken(user, deviceId) {
  const payload = { id: user.id, username: user.username, role: user.role };
  if (deviceId) payload.deviceId = deviceId;
  return jwt.sign(payload, JWT_SECRET, { expiresIn: JWT_EXPIRES_IN });
}

function generateRefreshToken() {
  return CryptoJS.lib.WordArray.random(32).toString();
}

function generatePartialToken(user) {
  return jwt.sign(
    { id: user.id, username: user.username, role: user.role, twofa_pending: true },
    JWT_SECRET,
    { expiresIn: '5m' }
  );
}

module.exports = {
  JWT_SECRET,
  JWT_EXPIRES_IN,
  REFRESH_TOKEN_DAYS,
  generateToken,
  generateAccessToken,
  generateRefreshToken,
  generatePartialToken
};
```

#### 5. `utils/site-url.js`
```javascript
function getSiteUrl(req) {
  if (process.env.SITE_URL) {
    return process.env.SITE_URL.replace(/\/+$/, '');
  }
  return `${req.protocol}://${req.get('host')}`;
}

module.exports = getSiteUrl;
```

#### 6. `utils/cdk-generator.js`
```javascript
const db = require('../api/db-sqlite');

const CDK_CHARS = 'ABCDEFGHJKMNPQRSTUVWXYZ23456789';

function generateCdkCode() {
  const sections = [];
  for (let s = 0; s < 3; s++) {
    let section = '';
    for (let i = 0; i < 4; i++) {
      section += CDK_CHARS[Math.floor(Math.random() * CDK_CHARS.length)];
    }
    sections.push(section);
  }
  return `PVE-${sections.join('-')}`;
}

function generateUniqueCdkCode() {
  let code;
  let attempts = 0;
  do {
    code = generateCdkCode();
    attempts++;
    if (attempts > 100) {
      throw new Error('无法生成唯一的 CDK 码，请重试');
    }
  } while (db.cdk.getByCode(code));
  return code;
}

module.exports = { generateCdkCode, generateUniqueCdkCode };
```

**验证**：拆分完这些工具后，在 `server.js` 顶部替换导入：
```javascript
// 替换前
const dbg = (...args) => { if (DEBUG) console.log('[DEBUG]', ...args); };
function _applyRate(key, statusData) { ... }
function createEmailTemplate(title, content) { ... }
// ... 其他工具函数

// 替换后
const dbg = require('./utils/debug');
const { _applyRate, clearExpiredCache } = require('./utils/pve-rate');
const { createEmailTemplate, sendEmail } = require('./utils/email');
const { generateToken, generateAccessToken, generateRefreshToken, generatePartialToken, JWT_SECRET } = require('./utils/token');
const getSiteUrl = require('./utils/site-url');
const { generateUniqueCdkCode } = require('./utils/cdk-generator');
```
启动服务测试，所有功能应完全正常。

---

### 第二步：拆分中间件
#### 1. `middleware/auth.js`
```javascript
const jwt = require('jsonwebtoken');
const { JWT_SECRET } = require('../utils/token');
const db = require('../api/db-sqlite');

const authMiddleware = (req, res, next) => {
  // 原代码完整复制
  const token = req.headers.authorization?.replace('Bearer ', '');
  if (!token) {
    return res.status(401).json({ error: '未授权' });
  }
  try {
    const decoded = jwt.verify(token, JWT_SECRET);
    if (decoded.twofa_pending) {
      return res.status(401).json({ error: '2FA 验证未完成' });
    }
    if (decoded.deviceId) {
      const device = db.refreshTokens.getById(decoded.deviceId);
      if (!device || device.revoked) {
        return res.status(401).json({ error: '该设备已被强制下线', code: 'TOKEN_EXPIRED' });
      }
    }
    req.user = decoded;
    next();
  } catch (err) {
    if (err.name === 'TokenExpiredError') {
      return res.status(401).json({ error: '令牌已过期', code: 'TOKEN_EXPIRED' });
    }
    return res.status(401).json({ error: '令牌无效' });
  }
};

const adminMiddleware = (req, res, next) => {
  if (req.user.role !== 'admin') {
    return res.status(403).json({ error: '需要管理员权限' });
  }
  next();
};

module.exports = { authMiddleware, adminMiddleware };
```

#### 2. `config/multer.js`
```javascript
const multer = require('multer');
const path = require('path');

const storage = multer.diskStorage({
  destination: (req, file, cb) => {
    cb(null, path.join(__dirname, '../images'));
  },
  filename: (req, file, cb) => {
    const ext = path.extname(file.originalname);
    const filename = `${Date.now()}-${Math.random().toString(36).substr(2, 9)}${ext}`;
    cb(null, filename);
  }
});

const fileFilter = (req, file, cb) => {
  const allowedTypes = /jpeg|jpg|png|gif|webp/;
  const extname = allowedTypes.test(path.extname(file.originalname).toLowerCase());
  const mimetype = allowedTypes.test(file.mimetype);

  if (extname && mimetype) {
    return cb(null, true);
  } else {
    cb(new Error('只允许上传图片文件'));
  }
};

const upload = multer({
  storage: storage,
  limits: { fileSize: 2 * 1024 * 1024 },
  fileFilter: fileFilter
});

module.exports = upload;
```

**验证**：在 `server.js` 中替换导入：
```javascript
// 替换前
const authMiddleware = (req, res, next) => { ... }
const adminMiddleware = (req, res, next) => { ... }
const upload = multer({ ... })

// 替换后
const { authMiddleware, adminMiddleware } = require('./middleware/auth');
const upload = require('./config/multer');
```
测试登录、权限验证、头像上传功能正常。

---

### 第三步：拆分路由（按业务模块）
**核心方法**：把 `server.js` 中所有 `app.METHOD()` 按业务模块剪切到对应路由文件，使用 `express.Router()` 导出

#### 示例：`routes/auth.js`（认证路由）
```javascript
const express = require('express');
const router = express.Router();
const CryptoJS = require('crypto-js');
const db = require('../api/db-sqlite');
const { generateAccessToken, generateRefreshToken, generatePartialToken, REFRESH_TOKEN_DAYS } = require('../utils/token');
const { authMiddleware } = require('../middleware/auth');

// 原 /api/login 接口完整复制
router.post('/login', async (req, res) => {
  const { username, password, device_name } = req.body;
  const hashedPassword = CryptoJS.SHA256(password).toString();
  
  const user = db.users.getByUsername(username);
  
  if (!user || user.password !== hashedPassword) {
    return res.status(401).json({ error: '用户名或密码不正确，请核对信息后重试' });
  }

  const refreshToken = generateRefreshToken();
  const ip = req.headers['x-forwarded-for'] || req.socket.remoteAddress || '';
  const ua = req.headers['user-agent'] || '';
  
  const record = db.refreshTokens.create({
    user_id: user.id,
    token: refreshToken,
    device_name: device_name || ua.substring(0, 100),
    ip,
    user_agent: ua,
    created_at: new Date().toISOString(),
    expires_at: new Date(Date.now() + REFRESH_TOKEN_DAYS * 24 * 60 * 60 * 1000).toISOString()
  });

  if (db.twofa.isEnabled(user.id)) {
    const partialToken = generatePartialToken(user);
    const { password: _, ...safeUser } = user;
    return res.json({
      twofa_required: true,
      partial_token: partialToken,
      refresh_token: refreshToken,
      user: safeUser
    });
  }
  
  const token = generateAccessToken(user, record.id);
  
  const { password: _, ...safeUser } = user;
  res.json({ token, refreshToken, user: safeUser });
});

// 原 /api/login/2fa 接口完整复制
router.post('/login/2fa', async (req, res) => {
  // 原代码完整复制
});

// 原 /api/auth/refresh 接口完整复制
router.post('/auth/refresh', async (req, res) => {
  // 原代码完整复制
});

// 原 /api/logout 接口完整复制
router.post('/logout', async (req, res) => {
  // 原代码完整复制
});

// 原 /api/auth/forgot-password 等密码重置接口完整复制
router.post('/auth/forgot-password', async (req, res) => {
  // 原代码完整复制
});

router.get('/auth/reset-password/:token', async (req, res) => {
  // 原代码完整复制
});

router.post('/auth/reset-password', async (req, res) => {
  // 原代码完整复制
});

module.exports = router;
```

#### 其他路由文件拆分对应关系
| 原 server.js 中的接口前缀 | 拆分到的路由文件 |
|--------------------------|------------------|
| `/api/user/2fa/*`        | `routes/user.js` |
| `/api/user/devices/*`    | `routes/user.js` |
| `/api/user/profile/*`    | `routes/user.js` |
| `/api/user/memos/*`      | `routes/user.js` |
| `/api/user/avatar`       | `routes/user.js` |
| `/api/user/email/*`      | `routes/user.js` |
| `/api/users/*`           | `routes/admin-user.js` |
| `/api/pve/vms`           | `routes/vm.js` |
| `/api/user/vms/*`        | `routes/vm.js` |
| `/api/vm/:vmid/*`        | `routes/vm.js` |
| `/api/pve/lxc`           | `routes/lxc.js` |
| `/api/user/lxc/*`        | `routes/lxc.js` |
| `/api/lxc/:vmid/*`       | `routes/lxc.js` |
| `/api/*/snapshots/*`     | `routes/snapshot.js` |
| `/api/admin/snapshot-config` | `routes/snapshot.js` |
| `/api/*/backups/*`       | `routes/backup.js` |
| `/api/admin/backup-config` | `routes/backup.js` |
| `/api/admin/backups/*`   | `routes/backup.js` |
| `/api/admin/cdk/*`       | `routes/cdk.js` |
| `/api/user/cdk/*`        | `routes/cdk.js` |
| `/api/messages/*`        | `routes/message.js` |
| `/api/admin/messages/*`  | `routes/message.js` |
| `/api/admin/smtp/*`      | `routes/admin-config.js` |
| `/api/admin/reminder/*`  | `routes/admin-config.js` |
| `/api/admin/storage`     | `routes/admin-config.js` |
| `/api/version`           | `routes/admin-config.js` |
| `/api/admin/network/config` | `routes/network.js` |
| `/api/ikuai/*`           | `routes/network.js` |
| `/api/port-forwards/*`   | `routes/network.js` |

**路由挂载**：在 `server.js` 中统一挂载所有路由：
```javascript
// 挂载路由
app.use('/api', require('./routes/auth'));
app.use('/api', require('./routes/user'));
app.use('/api', require('./routes/admin-user'));
app.use('/api', require('./routes/vm'));
app.use('/api', require('./routes/lxc'));
app.use('/api', require('./routes/snapshot'));
app.use('/api', require('./routes/backup'));
app.use('/api', require('./routes/cdk'));
app.use('/api', require('./routes/message'));
app.use('/api', require('./routes/admin-config'));
app.use('/api', require('./routes/network'));

// 保留最后一个通配符路由
app.get('*', (req, res) => {
  res.sendFile(path.join(__dirname, '../public', 'index.html'));
});
```

---

### 第四步：拆分 WebSocket 代理
#### 1. `websocket/vnc-proxy.js`
```javascript
const WebSocket = require('ws');
const https = require('https');
const net = require('net');
const tls = require('tls');
const crypto = require('crypto');
const pveApi = require('../api/pve-api');
const dbg = require('../utils/debug');

const vncProxy = new WebSocketServer({ noServer: true });

// 原 vncProxy.on('connection') 完整复制
vncProxy.on('connection', (clientWs, request) => {
  // 原代码完整复制
});

// 原 handleLxcVncTcpProxy 函数完整复制
function handleLxcVncTcpProxy(clientWs, pveHost, port, ticket) {
  // 原代码完整复制
}

module.exports = vncProxy;
```

#### 2. `websocket/terminal-proxy.js`
```javascript
const WebSocket = require('ws');
const { createTerminalPty } = require('../api/ssh-exec');
const dbg = require('../utils/debug');

const terminalProxy = new WebSocketServer({ noServer: true });

// 原 terminalProxy.on('connection') 完整复制
terminalProxy.on('connection', (clientWs, request) => {
  // 原代码完整复制
});

module.exports = terminalProxy;
```

**在 server.js 中替换**：
```javascript
// 替换前
const vncProxy = new WebSocketServer({ noServer: true });
const terminalProxy = new WebSocketServer({ noServer: true });
// ... 所有 WebSocket 相关代码

// 替换后
const vncProxy = require('./websocket/vnc-proxy');
const terminalProxy = require('./websocket/terminal-proxy');

// 保留 upgrade 事件处理（不变）
httpServer.on('upgrade', (request, socket, head) => {
  const url = new URL(request.url, `http://${request.headers.host}`);
  if (url.pathname === '/vnc-proxy') {
    vncProxy.handleUpgrade(request, socket, head, (ws) => {
      vncProxy.emit('connection', ws, request);
    });
  } else if (url.pathname === '/term-proxy') {
    terminalProxy.handleUpgrade(request, socket, head, (ws) => {
      terminalProxy.emit('connection', ws, request);
    });
  } else {
    socket.destroy();
  }
});
```

---

### 第五步：拆分服务层与定时任务
#### 1. `services/expiry-check.js`
```javascript
const db = require('../api/db-sqlite');
const pveApi = require('../api/pve-api');
const { createEmailTemplate, sendEmail } = require('../utils/email');
const dbg = require('../utils/debug');

let isCheckingExpired = false;
const reminderSentTracker = new Map();

// 原 loadSentRemindersFromDb 函数完整复制
async function loadSentRemindersFromDb() {
  // 原代码完整复制
}

// 原 checkExpiredVms 函数完整复制
const checkExpiredVms = async () => {
  // 原代码完整复制
};

// 原 checkExpiredLxc 函数完整复制
async function checkExpiredLxc() {
  // 原代码完整复制
}

module.exports = { loadSentRemindersFromDb, checkExpiredVms, checkExpiredLxc };
```

#### 2. `services/backup-polling.js`
```javascript
const db = require('../api/db-sqlite');
const pveApi = require('../api/pve-api');
const { createEmailTemplate, sendEmail } = require('../utils/email');
const dbg = require('../utils/debug');

const backupPollIntervals = new Map();
const lxcBackupPollingMap = new Map();
const lxcRestorePollingMap = new Map();

// 原 sendBackupNotification 函数完整复制
async function sendBackupNotification(userId, vmId, status, filename) {
  // 原代码完整复制
}

// 原 startBackupPolling 函数完整复制
function startBackupPolling(backupId, upid) {
  // 原代码完整复制
}

// 原 resumeRunningBackups 函数完整复制
function resumeRunningBackups() {
  // 原代码完整复制
}

// 原 sendRestoreNotification 函数完整复制
async function sendRestoreNotification(userId, vmId, statusMsg) {
  // 原代码完整复制
}

// 原 startRestorePolling 函数完整复制
function startRestorePolling(taskId, upid) {
  // 原代码完整复制
}

// 原 startLxcBackupPolling 函数完整复制
function startLxcBackupPolling(backupId, upid, vmid) {
  // 原代码完整复制
}

// 原 sendLxcBackupNotification 函数完整复制
async function sendLxcBackupNotification(vmid, backupId, status) {
  // 原代码完整复制
}

// 原 startLxcRestorePolling 函数完整复制
function startLxcRestorePolling(taskId, upid, vmid) {
  // 原代码完整复制
}

// 原 sendLxcRestoreNotification 函数完整复制
async function sendLxcRestoreNotification(vmid, taskId, status) {
  // 原代码完整复制
}

// 原 resumeRunningLxcBackups 函数完整复制
function resumeRunningLxcBackups() {
  // 原代码完整复制
}

module.exports = {
  startBackupPolling,
  resumeRunningBackups,
  startRestorePolling,
  startLxcBackupPolling,
  startLxcRestorePolling,
  resumeRunningLxcBackups
};
```

#### 3. `services/ikuai-sync.js`
```javascript
const db = require('../api/db-sqlite');
const pveApi = require('../api/pve-api');
const ikuaiApi = require('../api/ikuai-api');
const dbg = require('../utils/debug');

// 原 syncPortForwardsFromIkuai 函数完整复制
async function syncPortForwardsFromIkuai() {
  // 原代码完整复制
}

module.exports = { syncPortForwardsFromIkuai };
```

#### 4. `services/dhcp-static.js`
```javascript
const db = require('../api/db-sqlite');
const ikuaiApi = require('../api/ikuai-api');
const dbg = require('../utils/debug');

// 原 getWanInterface 函数完整复制
async function getWanInterface() {
  // 原代码完整复制
}

// 原 pickUnusedStaticIp 函数完整复制
async function pickUnusedStaticIp() {
  // 原代码完整复制
}

// 原 createDhcpStaticBinding 函数完整复制
async function createDhcpStaticBinding(type, vmid, mac) {
  // 原代码完整复制
}

// 原 removeDhcpStaticBinding 函数完整复制
async function removeDhcpStaticBinding(type, vmid) {
  // 原代码完整复制
}

module.exports = {
  getWanInterface,
  createDhcpStaticBinding,
  removeDhcpStaticBinding
};
```

#### 5. `schedule/tasks.js`
```javascript
const schedule = require('node-schedule');
const { checkExpiredVms, checkExpiredLxc } = require('../services/expiry-check');
const { clearExpiredCache } = require('../utils/pve-rate');

function initScheduledTasks() {
  // 每5分钟检查到期
  schedule.scheduleJob('*/5 * * * *', () => {
    checkExpiredVms();
    checkExpiredLxc();
  });

  // 每小时清理 PVE 缓存
  schedule.scheduleJob('0 * * * *', () => {
    clearExpiredCache();
  });
}

module.exports = { initScheduledTasks };
```

---

### 第六步：精简入口文件 server.js
最终 `server.js` 将精简为以下内容：
```javascript
const express = require('express');
const cors = require('cors');
const path = require('path');
const http = require('http');
require('dotenv').config();

// 核心依赖
const dbg = require('./utils/debug');
const pveApi = require('./api/pve-api');
const ikuaiApi = require('./api/ikuai-api');

// 中间件
const { authMiddleware, adminMiddleware } = require('./middleware/auth');
const upload = require('./config/multer');

// WebSocket
const vncProxy = require('./websocket/vnc-proxy');
const terminalProxy = require('./websocket/terminal-proxy');

// 服务
const { loadSentRemindersFromDb, checkExpiredVms, checkExpiredLxc } = require('./services/expiry-check');
const { resumeRunningBackups, resumeRunningLxcBackups } = require('./services/backup-polling');
const { syncPortForwardsFromIkuai } = require('./services/ikuai-sync');

// 定时任务
const { initScheduledTasks } = require('./schedule/tasks');

// 路由
const authRoutes = require('./routes/auth');
const userRoutes = require('./routes/user');
const adminUserRoutes = require('./routes/admin-user');
const vmRoutes = require('./routes/vm');
const lxcRoutes = require('./routes/lxc');
const snapshotRoutes = require('./routes/snapshot');
const backupRoutes = require('./routes/backup');
const cdkRoutes = require('./routes/cdk');
const messageRoutes = require('./routes/message');
const adminConfigRoutes = require('./routes/admin-config');
const networkRoutes = require('./routes/network');

const app = express();
const PORT = process.env.PORT || 3000;

// 全局中间件
app.use(cors());
app.use(express.json({ limit: '10mb' }));
app.use(express.urlencoded({ limit: '10mb', extended: true }));
app.use(express.static(path.join(__dirname, '../public')));
app.use('/images', express.static(path.join(__dirname, '../images')));

// 挂载路由
app.use('/api', authRoutes);
app.use('/api', userRoutes);
app.use('/api', adminUserRoutes);
app.use('/api', vmRoutes);
app.use('/api', lxcRoutes);
app.use('/api', snapshotRoutes);
app.use('/api', backupRoutes);
app.use('/api', cdkRoutes);
app.use('/api', messageRoutes);
app.use('/api', adminConfigRoutes);
app.use('/api', networkRoutes);

// 通配符路由
app.get('*', (req, res) => {
  res.sendFile(path.join(__dirname, '../public', 'index.html'));
});

// 创建 HTTP 服务器
const httpServer = http.createServer(app);

// WebSocket 升级处理
httpServer.on('upgrade', (request, socket, head) => {
  const url = new URL(request.url, `http://${request.headers.host}`);
  if (url.pathname === '/vnc-proxy') {
    vncProxy.handleUpgrade(request, socket, head, (ws) => {
      vncProxy.emit('connection', ws, request);
    });
  } else if (url.pathname === '/term-proxy') {
    terminalProxy.handleUpgrade(request, socket, head, (ws) => {
      terminalProxy.emit('connection', ws, request);
    });
  } else {
    socket.destroy();
  }
});

// 启动服务器
httpServer.listen(PORT, async () => {
  console.log(`服务器运行在 http://localhost:${PORT}`);
  
  // 初始化任务
  try {
    await pveApi.detectNode();
  } catch (error) {
    console.error('启动时检测节点失败:', error);
  }
  
  await loadSentRemindersFromDb();
  resumeRunningBackups();
  resumeRunningLxcBackups();
  
  // 启动时同步爱快端口转发
  setTimeout(async () => {
    if (ikuaiApi.isConfigured()) {
      try {
        await syncPortForwardsFromIkuai();
      } catch (e) {
        console.error('[端口转发] 启动同步失败:', e.message);
      }
    } else {
      console.log('[端口转发] ikuai 未配置，跳过启动同步');
    }
  }, 5000);
  
  // 初始化定时任务
  initScheduledTasks();
  
  // 首次执行到期检查
  checkExpiredVms();
  checkExpiredLxc();
});
```

## 四、拆分注意事项与验证步骤
### 关键注意事项
1. **导入路径必须正确**：所有 `require()` 路径要根据新的目录结构调整
2. **不要修改任何业务逻辑**：拆分过程中只做代码搬运，不做任何功能变更
3. **逐步验证**：每拆分一个模块就启动服务测试一次，不要一次性拆完再测
4. **备份原文件**：拆分前先备份 `server.js` 为 `server.js.bak`，出问题可随时回滚
5. **全局变量处理**：所有原来在 `server.js` 中定义的全局变量（如 `pveCounterCache`）要移到对应的工具/服务文件中

### 验证步骤
1. 拆分完工具函数和中间件后，测试登录、登出、权限验证
2. 拆分完一个路由模块后，测试该模块的所有接口
3. 拆分完 WebSocket 后，测试 VNC 和终端功能
4. 拆分完服务和定时任务后，测试到期提醒、备份恢复功能
5. 最后全面测试所有功能，确保与拆分前完全一致

## 五、拆分后的收益
1. **可维护性大幅提升**：修改某个功能只需找到对应的文件，不用在 2000 行代码里翻找
2. **多人协作更方便**：不同开发人员可以同时修改不同模块，不会产生冲突
3. **代码复用性提高**：工具函数和服务可以在多个地方复用
4. **便于后续扩展**：新增功能只需在对应模块添加，不会让入口文件越来越大
5. **调试更简单**：错误会指向具体的文件和行号，定位问题更快